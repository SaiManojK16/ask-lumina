/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'luminascreens.com',
      'lh3.googleusercontent.com',
      'www.svgrepo.com'
    ],
  },
};

export default nextConfig;
